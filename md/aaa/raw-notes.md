In a canvas doc, create me a design doc. The design doc is for an app called AAA. This is an app that is going to be an autonomous architectural agent. What it is, it's an open claw in the backend which triggers multiple agents to create a pipeline to generate architectural presentation boards. So with the new model, image model, GPT Image V2, I want to take this to the next level. How it's going to work is user give a initial prompt, whatever it may be, it will hand it off to a researcher agent that will clean up the prompt, do research when necessary, and then compile the final information to a couple of things. Like the first thing is like a design doc. I don't want you to to focus be on the pipeline and how it's getting it is more about the overall architecture and the actual details of the pipeline will be handled by the developer agents and as we go along Now I would like to move on to the front end so the front end there are two main modes that can be switched in the sidebar and in the sidebar you have the 1st logo you have to switch and then below the switch you have folders organized based on sessions and each session is a pipeline run so based on that that is the sidebar and you can switch between two modes the first mode is a pipeline builder and the second mode is the pipeline run you can rename these most be just one word things that clarify so the pipeline maker is like a infinite graph with the nodes think of like the node UIS like Grasshopper and Rhino Unreal Engine Blueprint or like the even nicer ones like Blender notes or like makes M8 so in the UI I want it to be more like an 8 and minimal clean very intuitive but what it is is going to be each node is going to be an agent and I can design which each node does how it hands off what it hands off what's the context what's the persisting data how is getting hand off WH and then I can modify like what's the instructions getting passed to each agent like what's the flow that it runs and like even like parallel runs and like sub agents I can spawn pacific sub agents and like how many sub ages running in parallel so that everything will be control through this node graphs and I can like add nodes delete nodes like basic stuff So all of this should be in the design doc as a list of features now in the chat mode once I start the chat I will be basically in like a group chat with like all the agents I have assigned in the pipeline so the pipeline and the chat mode is going to be linked and interacted Every time I add an agent the agent will appear in the pipeline chat uh yeah pipeline chat and then once I fire off a message the agents are going to just take over and then start the pipeline and then just go step by step through the agents the user may interrupt or the agent may ask the user for clarifying questions or to ask the user to add options select options and then go from there So I want to go back to the back end for the back end right now the back end data is based on SQL and the Prisma Prisma database and in terms of the architecture of the AG the workflow I'm thinking is first we will have a overall generation of the board the presentation board and with the image GPT image 2 and then we'll use thinking model to uh to reason through each image so first you generate the board second you have to make a segmentation progress a process to segment each uh drawing out of the board so you have a rendering you have like floor plan elevation section diagram so each of them should must be segmented and then pass through different uh sub agents to analyze like consist encies to analyze different things to check if this is like a coherent project so this will be a final check but that's not how you're gonna develop the overall concept I think concept would be developed better if you have like a overall idea of a project maybe is like a formal thing and then like start with a drawing or start with a floor plan start with a section and then start producing drama based on 1 ground truth of one floor plan or one section and then so for example like the placement of things will remain consistent and stuff like that so remember that so that's gonna be and after that once you have like a list of drawings then you can reconcile that into a whole presentation board So right now this seems like there are two conflicting ways to do this so I do want to think about this a little and see how this goes So right now just do a design doc or whatever we'll keep updating the design dock as we go But I want to do two things I want you to do two things do the design doc and then help me reconcile to these two uh pipeline option architectures 

There will be 3 modes, 1 is build, another is run, and last is explore. 

In the **chat mode**, like I mentioned before, it will retain the same sidebar, it will retain the same sessions, everything, but instead of the node graph, it will be just the chat UI. And the thing about the chat UI will be there will be also a right-side sidebar, and the chat will have the ability to create artifacts, like, you know, design documents, images, like it creates like a collection. So in the right sidebar, user can switch between file browser, project file browser, which contains all the project files, and then like individual file viewer slash editor, for example, if user opens an MD file, it will switch tabs and open the MD file within that tab inside the sidebar. So how I imagine the sidebar on the right will be, you have the little tabs vertically, like Adobe AutoCAD tab, so the texts are rotated 90 degrees, the tabs are rotated 90 degrees, just a vertical stack, and then you will have the project browser tab, the individual file explorer slash editor tab, and then one more tab of like general stuff of like which agents are within the this pipeline, and another tab, which is the terminal of like, okay, what is actually running the code. So this is very close to what is actually happening in the gateway, but the difference is this is going to be filtered out to show the agent related tasks only related to this pipeline run, not everything on the gateway, but also simplified in a way that the user can understand. So there are four tabs, the general tab, the two that I mentioned before, and the last one is the terminal. The general tab is somewhere I can like name the chat, I can check which agent is on, I can like mute the agent so it doesn't necessarily change the agent behavior, but it just hides the agent in the chat, so the agent, the message are not too crowded.   
When the other sidebar is in chat view but with the files tab selected. When files are selected, the sidebar will show a project file browser like a Revit project file browser. This will show the project file folder with all the files contained. There are two views to it, one version is just to filter out files in like an android file viewed so it is simplified with just file icons and another view is all the files explicit in the folder structure. 

I want to add an **explorer mode** in addition to chat and build this mode is is to validate the data essentially validate the ground true as a unified 3D model So that is the overall intent of this tab however the implementation is going to be a little different So for the UI first you will have a blender like interface with the on the right side we have a sidebar with layers tools everything and on the left side we'll still have the session viewer uh tab with this tab switch and it won't have a mixture like Blender like interface. The way it's actually gonna work is we're gonna have back end as rhino compute now we'll compute the geometry so first you will have the front end UI3D viewer you generate the the geometry by sending the command to rhino compute that will generate the compute generate the 3D model and bring it back to the web UI and present it in the web UI as a viewer so it will be partial control you have the viewer to go around make it spend viewer zoom into file but then if you need like the high level editing you have to send it back to right on computer and then see the 3D model but this is more for the agent rather than the viewer so the agent can compute and then do at whatever analysis it needs to be due in the back end and then have this as the ground truth say it cuts floor plans elevations based on this model and the agent can do its thing but the U tab is there for the user to see what the agent is looking at and the agent is working on so it happened 3D viewer to do that.

I guess the way I would want it ideally would be that to prompt something, the research agent, or maybe no, research agent is doing all the information necessary, whatever it can get. But the intelligence of the research agent is knowing what's the scope that I need to research. For example, like making certain assumptions, knowing what to ask the user, and then knowing what kind of question to ask the user, for example, like, instead of asking, oh, where is the site, you should be able to assume certain things based on the context of the project, and then ask the user something like, is it located in Southern California? If not, please clarify. Or something like, assumption will be made in SoCal. If it is not really relevant, if not, please clarify, you know. So be proactive with that. So with that research agent, it will create the necessary report with the compiled information. And the concept agent must be intelligent enough to create a ground truth for the project that a design development agent can take that concept and develop it into the final presentation for it. So concept development agent must understand, okay, what kind of concept are we developing? Is it, yeah, what kind of concept it is? So how do we intelligence that? What are my options? And from that, I think that the DD design development agent can easily develop the final presentation for using image too. So let's think about how the concept development agent will develop this concept. 